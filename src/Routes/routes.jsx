/*
    ? All the routes of the platforms will be defined in this file, 
    ? so in case of change in the name of the routes the impact on the other screen 
    ? will be minimal or almost none!
*/

export const HOME = "/";